import ChatAppPage from "@/registry/new-york/blocks/chat-app/page";

/**
 * A live route for the chat app block. The registry ships the same page at
 * `app/chat/page.tsx` for installers; this route mounts that source unchanged so the
 * docs can link a working surface instead of describing one.
 */
export default ChatAppPage;
